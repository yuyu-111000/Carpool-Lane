# 执行计划：Carpool Lane《挪车！师傅》三日冲刺

> v1.0 · 2026-08-28 · 仓库 https://github.com/yuyu-111000/Carpool-Lane-260831.git
> 主线：Day1 核心循环+验证门 → Day2 50关生产+元系统 → Day3 打磨+交付。每个任务带验证标准，做完即 commit。

## 全局纪律

- **验证门不过不前进**：G1（3 关还想玩吗）、G2（捎人是否加乐趣）是硬门
- **每个任务完成 = 测试过 + commit**；commit message 中文、一行说清"做了什么+验证了什么"
- 每日收尾：更新迭代日志（docs/06）、push、记录次日风险
- 求解器与游戏共用 `src/rules/`，改 rules 必跑 T1

## Phase 0：骨架（0.5h）

- [ ] T0.1 目录骨架：game/src/{rules,}, game/tools/, game/tests/, dist/
- [ ] T0.2 .gitignore（node_modules/dist 冗余）
- [ ] 验证：`node --version` 可用；目录结构与技术方案 §三 一致

## Day 1：核心循环（今天）

### 上午：规则内核（TDD）

- [ ] T1.1 `src/rules/board.js`：棋盘解析、车辆坐标、占用网格、状态编码/克隆/相等
  - 验证：node:test 单测（构造/越界/冲突检测）
- [ ] T1.2 `src/rules/move.js`：合法移动枚举与执行；班车同步推进（moveCar → 班车位移）
  - 验证：单测覆盖边界（贴墙、互挡、班车被挡停）
- [ ] T1.3 `src/rules/goal.js`：目标谓词（红车出界；捎人版=乘客全接+红车出界；按序接送）
  - 验证：单测三种目标形态
- [ ] T1.4 `tools/solver.js`：双向 BFS（可解性+最优步数）+ 每车必要性 + 解路径窄度
  - 验证：**Fogleman 基准回归测试 20 题**（tools 下载子集内嵌 tests/fogleman.fixture.json）；手工构造 3 个已知答案的捎人/班车关卡
- [ ] T1.5 `tools/build.js`：ESM 模块拼接为单文件 dist/index.html（IIFE 包裹）
  - 验证：拼接产物在浏览器跑通无 console 错误

### 下午：可玩原型

- [ ] T1.6 `src/render.js`：Canvas 棋盘/车辆/出口/乘客/道闸绘制（emoji 车辆+高对比配色）
- [ ] T1.7 `src/input.js`：指针拖动（网格吸附）+ 点击两模式；touch-action:none
- [ ] T1.8 `src/main.js`：场景状态机 boot→title→level；关卡数据内嵌 3 关；胜利判定→结算（步数/星级）
- [ ] T1.9 **验证门 G1**：3 关纯滑块试玩——首关 10 秒可解？拖动跟手？"还想继续吗"？
  - 不过 → 调手感参数（吸附阈值/缓动）再测；连过 2 轮仍不过 → 回炉规则
- [ ] T1.10 捎人机制 3 关（手摆，验证用）→ **验证门 G2**：捎人=规划乐趣还是跑腿拖沓？
  - 结论写入迭代日志；决定第 2 章乘客位设计基调
- [ ] T1.11 commit + push；更新迭代日志 Day1 段

## Day 2：50 关生产 + 元系统

### 上午：管线

- [ ] T2.1 `tools/generator.js`：按章节配置 rejection sampling（尺寸/车数/乘客/班车/墙）
- [ ] T2.2 `tools/pipeline.js`：生成→solver 验证→双指标筛选→50 关 + difficulty-curve.svg + run 报告
  - 验证：50 关全过 validate（可解/par 一致/必要性/曲线单调）；**T2 CI 门**
- [ ] T2.3 人工抽检每章 3 关（记录体感 vs 双指标偏差）

### 下午：游戏全量

- [ ] T2.4 机制全量：班车接入（G2 结论决定去留）、固定墙、7×7、按序接送
- [ ] T2.5 `src/meta.js`：章节地图/星级/streak/存档/选关；`?free=1` 全开参数
- [ ] T2.6 `src/hint.js`：内置 AI 提示（迷你 BFS 高亮下一辆车）
- [ ] T2.7 每日挑战（种子随机）——时间不够按砍序跳过
- [ ] T2.8 commit + push；迭代日志 Day2 段

## Day 3：打磨交付

- [ ] T3.1 `src/fx.js`：通关动画（道闸+冲出+粒子）、捎人上车、音效（WebAudio 合成）、combo
- [ ] T3.2 **T3 手感冒烟门**：webapp-testing（Playwright）加载 dist——截图存档+console 零错误+模拟拖动过第 1 关；手机 Safari/Chrome 真机
- [ ] T3.3 **T4 试玩门**：2-3 人试玩，记录卡点/情绪，修 Top3 问题
- [ ] T3.4 设计思路文档（docs/07）+ 迭代日志定稿（docs/06）
- [ ] T3.5 交付打包：dist/index.html + 两文档；自检清单全绿 → 邮件材料
- [ ] T3.6 最终 commit + push + tag v1.0

## 砍序（工期溢出时，按此序砍，不留恋）

班车机制 → 7×7 大棋盘 → 每日挑战 → combo 反馈 → 装饰粒子

## 里程碑与门禁总览

| 门 | 时机 | 通过标准 |
|---|---|---|
| T1 单测 | 改 rules/solver 后 | node:test 全绿 + Fogleman 20/20 |
| T2 关卡 CI | 关卡数据变更后 | validate 50/50 通过 |
| G1 | Day1 晚 | 3 关自测"还想继续" |
| G2 | Day1 晚 | 捎人=乐趣结论明确 |
| T3 | Day3 | Playwright 冒烟 + 真机零错误 |
| T4 | Day3 | 试玩反馈闭环（修完 Top3） |
